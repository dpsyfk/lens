[package]
name = "lens-platform"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
lens-core = { path = "../lens-core" }
lens-tls = { path = "../lens-tls" }
