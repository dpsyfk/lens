[package]
name = "lens-tui"
version = "0.1.0"
edition = "2021"
publish = false
license = "Apache-2.0"

[lib]
path = "src/lib.rs"

[dependencies]
crossterm = "0.29"
lens-core = { path = "../lens-core" }
lens-store = { path = "../lens-store" }
ratatui = "0.29"
